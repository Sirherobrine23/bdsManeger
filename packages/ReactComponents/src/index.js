import Node_Fetch from "node-fetch";
import socket_io_client from "socket.io-client";

export default function Auth(Host = "localhost", Port = 1932, Auth = {}) {
  const MainModule =  {};
  return MainModule;
}
